import {
    ClerkProvider,
    SignInButton,
    SignedIn,
    SignedOut,
    UserButton
  } from '@clerk/nextjs'
const authentication = () => {
    return (
        <div>
            
        </div>
    );
};

export default authentication;